[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m flask run --host=0.0.0.0 --port=$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 10

[env]
PYTHON_VERSION = "3.11"
FLASK_APP = "health.py" 