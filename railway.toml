[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "alembic upgrade head && python -m bot"
healthcheckPath = "/health"
healthcheckTimeout = 300
healthcheckInterval = 60
restartPolicy = "on-failure"

[env]
PYTHON_VERSION = "3.11"
PORT = "8000" 