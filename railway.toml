[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn bot.__main__:create_app_sync() --bind 0.0.0.0:$PORT --worker-class aiohttp.GunicornWebWorker --workers 1"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicy = "always"

[env]
PYTHON_VERSION = "3.11"
PORT = "8000" 